# Research Timeline — AI_DNA_ANALYZER

Reconstructed from devlogs (`History/`), benchmark summaries (`results/`), experiment reports
(`experimental/`, `VALIDATION_ROUND_2.md`, `FINAL_RESEARCH_REPORT.md`), the unmerged developer
worktrees under `.claude/worktrees/`, git history, file modification times, and direct
re-computation from cached arrays where a document was ambiguous. Dates are 2026. Where a date is
inferred from file modification times or commit dates rather than stated in a devlog, it is marked
`~`. Only events that changed the scientific interpretation are listed; each carries the
Observation → Hypothesis → Experiment → Result → Correction → New hypothesis chain where
the evidence supports one.

Source shorthand: `D<n>` = `History/<n>_DEVLOG.md` (main numbering; the router-freeze worktree
`agent-aaff295c27efb0e79` uses a *different* numbering for its devlogs 10–16 — see E14);
`VR2` = `VALIDATION_ROUND_2.md`; `FRR` = `FINAL_RESEARCH_REPORT.md`; `R3` =
`FOUR_EXPERIMENT_VALIDATION_REPORT.md` (unmerged worktree `agent-a5e73df68b249ba24`);
`PERF` = `experimental/performance/PERFORMANCE_OPTIMIZATION_REPORT.md` (unmerged worktree
`agent-a4cedfdeffb76e3dd`); `HTS` = `HTSLIB_IO_PERFORMANCE_BENCHMARK.md` + `bench_htslib_io/*.md`
(unmerged worktree `agent-afd5734475c9603bd`); `CHI` = `experimental/performance/CHTSLIB_INTEGRATION_REPORT.md`.

---

## Phase 0 — Neural-caller prototype on synthetic reads (Aug 4–10)

| # | Date | Event | Evidence | Why it matters |
|---|---|---|---|---|
| E1 | 08-04→08-06 | **Original architecture:** a reference-guided bidirectional "Mamba-style" token classifier (`model.py`; a gated causal-convolution approximation, *not* a true selective SSM) predicting Normal/SNP/Insertion/Deletion per base. Initial hypothesis (D3): mutation density ~1:1000, het VAF ≈ 0.45–0.55. Data = **synthetic reads** (`generate_mock_bam.py`) spliced from `generate_mock_vcf.py` variants onto a real HG002 chr21 reference slice. | git `13df7de`…`064c16c`; `model.py:1-27`; D3:1-64 | The neural phase never measured real-read HG002 performance. |
| E2 | 08-07 | Two data bugs found: mock BAM spliced only SNPs (indel classes physically unsolvable); `vocab_size=5` missing the GAP token. After fixes, SNP precision 0.02–0.07 at every epoch. | D3:10-45 | First instance of "data/evaluation artefact masquerading as model failure". |
| E3 | 08-07 | Halving the SNP class weight appeared to fix it (macro-F1 0.70→0.92/0.95) but collapsed again on both seeds. An unnamed "third-party review" recommending a decision threshold instead of argmax was dismissed as excessive. | D3:66-156 | The dismissed recommendation was later vindicated (E4). |
| E4 | 08-08 | **Threshold artefact:** the same epoch-5 checkpoint scored macro-F1 0.0884 at threshold 0.5 and 0.9095 at 0.9; threshold-free AUC Δ = +0.006, CI [−0.010, +0.022] (p = 0.46). Fixed 0.5 threshold sat 0.01 above the 99.99th percentile of a drifting Normal-class distribution. | DEVLOG_2026-08-08:27-61 | "Collapse" was an evaluation rule, not degradation. AUC/AP added next to thresholded metrics. |
| E5 | 08-09→08-10 | **Recall ceiling 0.7834 = data bug:** `generate_mock_vcf.py:21` chose REF at random (25.1 % matched the FASTA); 81/374 SNP labels were "phantoms" with no read evidence. Fix (REF read from FASTA); devlog declared *all earlier metrics non-comparable*. | D3:158-340; D4:1-82 | Explicit comparability break — history not silently overwritten. |
| E6 | 08-10 | Lowering focal γ 3.0→1.0 did not slow probability drift (negative on both seeds); fixed-0.5 rule "disproven three times"; checkpoint selection moved to per-epoch optimal threshold. | D5:1-113 | Negative result; neural line continues to stall. |

## Phase 1 — Real reads: statistical baselines beat neural models (Aug 11–13)

| # | Date | Event | Evidence | Why it matters |
|---|---|---|---|---|
| E7 | 08-11 | Real HG002 chr21 15x reads. Residual-Mamba initialised at the binomial LLR: **NULL** (0 disagreements at init; no trained epoch beat untrained; a "best-trained" +0.0038 ΔF1 did not replicate on another seed: +0.0000). Moving the binomial threshold 10.5→9.5 beat the network. Read-level Mamba: **NEGATIVE** (ΔF1 −0.076; decoupling base from read attributes *improved* F1 +0.043). | D6:209-240; D7:196-252 | Fixed-ε binomial baseline is hard to beat at 15x; motivates better *statistics*, not a bigger network. |
| E8 | 08-12 | **Quality-aware error model — POSITIVE.** Per-read ε_i = 10^(−Q_i/10) with an exact log-space Poisson-binomial DP: ΔF1 vs fixed-ε binomial = **+0.0179 [+0.0102, +0.0263]** (PB test F1 0.9908 vs 0.9729). Controls (global ε, shuffled ε) null ⇒ the *per-locus pairing* matters. Cost: PB 55.6 s vs binomial 0.16 s on 455 k loci. | D8:85-198 | The origin of the "expensive but better" model the cascade later economises. |
| E9 | 08-12 | Neural residual on top of PB with 44-channel evidence: **NULL** (byte-identical calls). Powered 15× benchmark (3.81 Mb test): PB-residual **WEAK POSITIVE** (2/3 seeds), left on side branch `worktree-30x-pb-residual`. | D9:106-190; D10 (branch numbering) | Neural line abandoned in the final method; `cascade.py` still contains a dormant third (Mamba) stage that is *not* part of any reported result. |
| E10 | 08-13 | **Router freeze (on a worktree branch, commit `c06deec`).** Thresholds selected on HG002 chr21:32–40 Mb at ~15× (subsample 0.203, seed 20260811): validation blocks 1,5 (1.91 Mb, 2,410 SNP) → binomial threshold **7.0**, PB threshold **10.5**; router cutoff **5.411872376933351** = 0.1 % validation quantile of −|LLR−7.0| under a pre-registered "smallest coverage within 0.0005 F1 of PB-everywhere" rule; test blocks (3.81 Mb, 5,704 SNP) scored once. A learned logistic router lost to this trivial rule. | worktree devlog 11 §2-3, 13 §7; router-freeze WT13 §7, §18 | **Provenance of all frozen constants.** Main-branch devlog 11 gives a different (incorrect) derivation ("F-beta on a training split") — the worktree record is primary. |

## Phase 2 — Cascade validation, and the first failures (Aug 14–19)

| # | Date | Event | Evidence | Why it matters |
|---|---|---|---|---|
| E11 | 08-14 | D11: frozen cascade on 7 chr21 regions (19.4 M loci): cascade = PB (ΔF1 +7×10⁻⁷; 2 disagreements). D12 (bench_v12): a **PB depth-clipping defect** found by accident at ~69× (PB F1 0.684, all 426 missed SNPs had `pb_llr == 0.0`). Stage-2 (PB→Mamba) and Stage-3 cascades NEGATIVE. | D11; D12 §7,15 | PB was silently broken above 48× depth; the "cascade beats PB at full depth" result was luck. |
| E12 | 08-14, 08-16 | **Coordinate caveat documented as benign.** D12 §16.5: cache `positions` are "cache-order indices offset by the chunk start, not true genomic coordinates" — "safe for the index-mapping this benchmark does". Repeated in D13 §22.9. Head-to-head work later found joining these positions to the truth VCF recovers <2 % of SNPs (E22). | D12:657-661; D13:797-799 | *Known-and-classified-as-safe* became a real defect only when a consumer needed genomic coordinates (E22, E23, E27). |
| E13 | 08-16 | D13: root cause of E11 = `np.clip(k, 0, tensor_width)` in `poisson_binomial_llr` → `−inf − −inf = nan → 0.0`; fixed (recount ALT over retained reads); bit-identical at depth ≤ tensor width; 1,371 true SNPs recovered at 70×. Pre-registered verdict rules written; a rule defect (|ΔF1| ≥ 0.001 also fires for *positive* ΔF1) disclosed. **Claim made: `router_fn` = 0 — "the cascade never lost a true SNP that PB found" (24.6 M chr21 loci).** | D13 §8-13, §17 | The claim was true only for chr21/HG002 — refuted next day (E14). |
| E14 | 08-17 | **D14 cross-chromosome (bench_v14):** four 1-Mb windows × three depths. Accuracy equivalent in 11/12 cells; pooled ΔF1 +0.000581 [+0.000075, +0.001089]; **pre-registered verdict NEGATIVE** because one powered cell (chr20 full: 2 extra FPs, ΔF1 −0.001262) tripped the |ΔF1| ≥ 0.001 clause. **5 cascade-specific true-SNP losses (4 distinct loci) in chr1 segmental duplication**: VAF 0.110–0.125, binomial LLR −5.3…+0.9 (just below the router band), PB LLR 11.9–24.1. | D14 §17 and verdict section; `results/bench_v14/disagreements.json` | Falsifies "never loses a true variant". Establishes the low-VAF/segdup failure mode. |
| E15 | 08-17 | **D15 ε-sensitivity safety layer (bench_v15).** Hypothesis: re-evaluating the binomial LLR at bracketing ε flags "confidently wrong" loci cheaply. Result on a held-out test set (chr16/15/7/14, 9.5 M loci): detector captured 74/80 cascade–PB departures (92.5 %) and the lost SNP, but escalating them to PB **fixed 5 calls and broke 69** (ΔF1(safety − cascade) = −0.0027). Formal verdict "WEAK POSITIVE" (fewer than 3 test failures); scientific decision: **rejected**. 50 candidate rules; grid informed by validation data (disclosed). | D15 §13, §18, §20 | "The detector works and the intervention does not": in segdup PB is *worse* than the binomial. |
| E16 | 08-18→08-19 | v16 (chr13, median difficulty): 0 lost true SNPs, ΔF1 −0.000035, PRESERVED. v17 (chr17:18–21 Mb, hardest untouched contig): ΔF1 +0.001892; **2 further true-SNP losses** (chr17:18,640,872 and 18,461,522; VAF 0.129/0.115). Cumulative HG002 "7" = 5 (v14) + 2 (v17). | D16, D17 | The 7-event figure quoted in earlier consolidated reports. |

## Phase 3 — Cross-sample validation and the first consolidations (Sep 4–16)

| # | Date | Event | Evidence | Why it matters |
|---|---|---|---|---|
| E17 | 09-04 | v18: HG003 (father), same chr13 window → 0 lost SNPs; 7 disagreements (5 in one ~200-bp segdup cluster). v19 extracted for HG004 (mother; chr2/3/5 × 3 depths). HG005 and HG001 "platform-matched" products returned HTTP 404 → HG004 substituted (disclosed as a weaker test). | D18; VR2 §2-3 | Related-trio caveat; only weak independence. |
| E18 | 09-14 | v19 scored: pooled ΔF1 +0.001014 (locus CI [+0.000787, +0.001254]; 50-kb block CI [+0.000520, +0.001601], 2.3× wider); **1 more true-SNP loss** (chr5:29,487,976; VAF 0.13, BQ 37.3) + 3 cascade-introduced FPs (BQ 18.8–24.6). Measured end-to-end (BAM→calls, 500 kb): **1.65×** (PB-only 470 s vs cascade 285 s) vs projected caller-stage 190–364×. VR2 wrongly calls this the "first" cascade-caused SNP loss. | VR2 §14; `results/bench_v19/` | First *measured* end-to-end evidence that extraction dominates. Routed-fraction ≠ CPU saving. |
| E19 | 09-15 | **htslib-internal threading is null:** `pysam threads=1/2/4/8` → 15.25/16.23/15.74/16.00 s (`load_counts`, 300 kb, n=3). Line profiling: "death by a thousand cuts" — no single line > 9–12 % (per-read pysam property access 55–60 %; `NM` tag scan 9.3 % of `_read_row`; BLAKE2b read-name hash 11.8 % of `_locus_reads`); htslib decompression ≈ 0.06 % of runtime. Region-chunked multiprocessing: 3.10× (`load_counts`, 0.6 Mb), 3.06× (`load_reads`, 0.3 Mb), 2.85× end-to-end (2 Mb, median of 2–3 noisy reps), all byte-identical. | HTS | Bottleneck is per-read Python/Cython crossing, not I/O. Multiprocessing kept experimental. |
| E20 | 09-15 | **Round 3 (unmerged worktree):** HG004 re-scored with independent block bootstrap: 7/9 cells PRESERVED, 2 IMPROVED (chr3), 0 DEGRADED; one true-SNP loss (same event, but reported at chr5:29,464,488 — a naive-coordinate value; see E27). GIAB v3.1 routing enrichment: lowmap_segdup 2.38 [1.64, 3.46], alldifficult 1.82 [1.31, 2.52], tandemrepeats 0.78 [0.42, 1.44] (no enrichment). Multiprocessing 5.34× at 8 workers (1 Mb; decision-identical). DeepVariant pull **blocked** (3.2 GB image vs 4.9 GB free disk). | R3 | Uses re-read HG004 bytes (not fresh data). |
| E21 | 09-16 | FRR consolidates history (thresholds, verdicts, rescue on HG004: 504 rescuable / 500 rescued; "false-rescue" 90.5 %). Records HG005/DeepVariant as blocked by a disk gate and "no hap.py cross-validation". Head-to-head external callers subsequently run at HG002 chr21:32–44 Mb, 15×: DeepVariant 0.9494, Clair3 0.9290, GATK 0.9682 (F1, hap.py); Strelka2 blocked (bundled htslib crashes on host zlib); FreeBayes not run. AI callers were first scored with the *internal* evaluator (F1 0.9741) — not comparable. | FRR; `experimental/head_to_head/` | The "HG005 blocked / no hap.py" statements in FRR/PDF were superseded within two days. |
| E22 | 09-16 | Head-to-head locus-level overlap analysis **discarded**: joining the cache `positions` to the truth VCF recovered < 2 % of truth SNPs (forced join gave DeepVariant TP = 33 vs true 16,313). Reported honestly; no numbers built on it. | `head_to_head/ERROR_ANALYSIS.csv` | Second sighting of the coordinate defect (first: E12). |

## Phase 4 — hap.py, genotypes, coordinates (Sep 17)

| # | Date | Event | Evidence | Why it matters |
|---|---|---|---|---|
| E23 | 09-17 | **Unified hap.py benchmark.** AI calls converted to VCFs with true coordinates (recovered from window tiling, *not* the cache `positions`; `SANITY_CHECK.md`: naive vs true diverge at index 5,568 after a 3-window drop) and scored by the identical hap.py: **F1 0.6238**, versus 0.9742 under the internal evaluator. Root cause (full population): writer forced GT=0/1 for every call; 5,630/5,777 (97.5 %) truth hom-alt SNPs were called with the right allele but wrong genotype = both an FN and an FP (85.2 % of all FNs); plus a 16,597 vs 16,898 truth-denominator difference (window-level BED filtering, 301 SNPs). | `unified_happy/ROOT_CAUSE_ANALYSIS.md` | The first hap.py number was misleading; the two evaluators measure different concepts. |
| E24 | 09-17 | **Genotype layer.** Method A (always 0/1): GT accuracy 0.6464 on hold-out. Method B (VAF > τ=0.69 ⇒ 1/1; τ fit on a 0.47-Mb dev region, 549 loci) failed to generalise: 1,195 heterozygous truth sites (11.6 % of hets) called 1/1 → GT accuracy 0.9250, hap.py F1 0.8921. **Method C** (binomial likelihood argmax over {0/0, 0/1, 1/1}, ε = 0.01 fixed, not fit): GT accuracy 0.9888, F1 0.9538 (hold-out call set 16,124) / 0.9547 (full-scope call set 16,094). Reference scipy loop 5,701 ms → vectorised 5.5 ms (1027×; 0 GT/GQ mismatches). Allele call set unchanged (symmetric difference 0). | `genotype_layer/RESULTS*.md`; `unified_happy/METHOD_C_FULL_REVALIDATION.md` | Closes +0.331 F1 of an *evaluation-representation* gap; no detection change. |

## Phase 5 — HG005, the extraction bug, native C (Sep 18–19)

| # | Date | Event | Evidence | Why it matters |
|---|---|---|---|---|
| E25 | 09-18 (`~`) | **Native C/htslib extension developed and validated in an isolated worktree** (`agent-a4cedfdeffb76e3dd`): single `bam_mplp_*` streaming pass per region. **First attempt failed equivalence** (up to 282,335 / 1,684,800 cells differing on a 200-kb check): pysam's `pileup()` silently applies `ignore_orphans=True` and `ignore_overlaps=True`; fixed via `bam_mplp_init_overlaps` + orphan-pair filter. Full 12 Mb HG002 region: 0 / 109,813,120 cell mismatches; VCF data records byte-identical; hap.py F1 identical to 6 decimals. Measured 1092.07 s → 16.76 s (65.1×). The report's "65.1× end-to-end" and "17–80× faster than DeepVariant/Clair3/GATK" statements used *frozen PB masks* and excluded `load_reads`/PB — **not supported as pipeline claims**. Never integrated. | PERF §7-10 | Real result; overclaimed framing. |
| E26 | 09-18 (`~`) | **HG005 (unrelated sample) stress test, chr1:1.0–4.0 Mb** (NHGRI 300× novoalign BAM, ≤250-bp reads, downsampled 9 % seed 42 → ~30×). hap.py F1 = **0.0085** (TRUTH.TOTAL 28,435; 70–78 % of loci carried a wrong REF for their recorded coordinate). Result **rejected as a scientific finding** and investigated rather than reported; DeepVariant/GATK/Clair3 not run. | `HG005_MAX_FEASIBLE_STRESS_TEST.md` | Human decision to distrust an implausible number. |
| E27 | 09-19 | **Coordinate bug root-caused and fixed.** `extract_bench_v12.py` wrote `positions = chunk_start + arange(n_rows)`, valid only if no 64-bp window was dropped by the BED/N filter (`providers._build_windows` drops whole windows). Fix: `load_counts(..., return_positions=True)` returns `window.start + offset` per row (3 call sites). Verification: 0 / 2,403,008 reference mismatches on HG005 (pre-fix 71.8 % mismatch on a 75,072-locus probe); byte-identical repeat extraction; 0 / 3,989 and 0 / 3,991 VCF REF mismatches; `counts`/`labels` byte-identical pre/post fix on real HG004; historical audit: **blast radius = one script** (`run_hg005_pipeline.py`) — every earlier benchmark either ignored absolute position or already rebuilt it (`genomic_positions()` since v14). 11 regression tests. | `HG005_EXTRACTION_BUG_ROOT_CAUSE.md`, `HG005_HISTORICAL_AUDIT.md` | Proves what was and was not affected. |
| E28 | 09-19 | **hap.py scope error caught in-session:** first post-fix hap.py reused the 24-Mb truth against a 3-Mb query (TRUTH.TOTAL 28,435; F1 0.237, "DIAGNOSTIC ONLY"); corrected with coordinate-clipped truth/BED. **Valid HG005 result:** PB-only P 0.9642 / R 0.9403 / F1 0.9521; cascade 0.9637 / 0.9403 / 0.9519; 2 disagreements (both cascade FPs); no true SNP lost. | `HG005_POSTFIX_STRESS_TEST_REPORT.md` | Region-scope mismatch is a standing hazard for regional benchmarks. |
| E29 | 09-19 | **Provenance incident (audit error).** A re-audit searched only the threading worktree (`agent-afd…`) and wrote that *no* C/htslib code existed anywhere (`PIPELINE_INTEGRITY_AUDIT.md` I-2, H-1) and that "~65×" was unsubstantiated. The human directed a re-check; the real implementation was found in `agent-a4ce…`, audited, rebuilt from source in the fix worktree, independently re-verified on HG002 and HG005, and wired into `pileup_counts.load_counts`. | audit "Correction" section; `AI_ASSISTANCE_DISCLOSURE.md` | An AI-assisted audit was wrong in both directions; human oversight caught it. |
| E30 | 09-19 | **Integration edge case:** `load_counts(return_positions=True)` raised `ValueError: need at least one array to concatenate` when all windows were dropped (pre-existing `np.concatenate([])`); fixed to return empty arrays; covered by a test. | CHI Phase 2 | Robustness fix; the same edge case was independently noted in the multiprocessing work (E19). |
| E31 | 09-19 | **Final regression and performance.** 102/102 tests as reported at the time (90 pre-existing + 12 native-equivalence; re-run 2026-09-19: 82.2 s) — **historical figure**: two of those test files were later lost and cannot be re-run (see E32, E36). HG005 3 Mb pipeline: `load_counts` 344 s → 15 s (22.9×) but `load_reads` 696 → 775 s and PB 351 → 349 s unchanged ⇒ **1392 s → 1140 s (1.22×)**; cache byte-identical to the validated post-fix cache (all nine arrays re-verified). Bottleneck migrated: `load_reads` + PB = 98.7 % of the new wall time. | CHI Part V; `PERFORMANCE_RESULTS.csv`; own re-check | The 65× extraction result does not transfer to the pipeline. |

## Phase 6 — `load_reads`, chr20, audit and freeze (Sep 20)

| # | Date | Event | Evidence | Why it matters |
|---|---|---|---|---|
| E32 | 09-20 | **Reboot wiped `/tmp`** (the worktree with uncommitted native code, HG005 post-fix raw artefacts, `experimental/performance/` reports, two test files). Work re-created in `.claude/worktrees/opt-chr20`; equivalence hashes of the lost run reproduced. | `research/LOAD_READS_OPTIMIZATION_REPORT.md`, `CHR20_VALIDATION_REPORT.md` | HG005 numbers now "preserved record"; two tests lost |
| E33 | 09-20 | **Native `load_reads`**: 26.9× (HG005 30×), 35.1× (HG002 15×) function-level; pipeline 2.72× serial, 7.11× with 4 processes; PB ≈ 93–96 % of compute; Python threads 0.73×; async 1.21×/1.05× pipeline-level only. First single-pass design failed with 30 mismatching cells and was replaced by per-window passes; 0 mismatches in > 6.7×10⁸ cells for the final design. | `LOAD_READS_OPTIMIZATION_REPORT.md` | keep function-level vs pipeline-level distinct |
| E34 | 09-20 | **chr20 whole-chromosome validation** (HG002, 15×, frozen constants, 56.2 M loci): ΔF1 −5.64×10⁻⁵ [−1.05×10⁻⁴, −1.4×10⁻⁵], 10 discordant loci, verdict **DEGRADED** (extremely small magnitude); 8 low-BQ cascade FPs, 1 lost true SNP (chr20:46,110,517), 1 FP avoided; hap.py F1 0.96285 vs 0.96280. | `CHR20_VALIDATION_REPORT.md`, `results/bench_v20/` | first strict DEGRADED at chromosome scale; not relabelled |
| E35 | 09-20 | **Forensic audit** of manuscript and package. | `FINAL_FORENSIC_AUDIT.md` | list of corrections |
| E36 | 09-20 | **Freeze corrections**: chr20 and `load_reads` added to the manuscript; global "CONFIRMED" removed; hard-coded paths removed; `.pyc` untracked; invalidated pre-fix HG005 outputs quarantined; three tests reconstructed. While doing so, the native **counts** backend was found to differ from per-window passes on adversarial synthetic BAMs (realistic BAMs and real data equal); documented, code unchanged. | `FINAL_FREEZE_REPORT.md` | no scientific number changed |


## Not resolved
* M-1 (window-level vs SNP-level BED filtering): open; quantified in the manuscript §3.3 / `LIMITATIONS_AND_OPEN_QUESTIONS.md`.
* No lockfile; native work, chr20 material and `research/` uncommitted (main working tree; copy in `.claude/worktrees/opt-chr20`); no genome-scale run and only one chromosome-scale run (HG002 chr20, 15×); no matched-scope runtime comparison with any external caller; raw HG005 post-fix artefacts unavailable.
